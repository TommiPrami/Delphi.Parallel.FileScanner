// Filename: DPFSUnit.Parallel.FileScanner.inc

// Comment out to use the RTL PPL (System.Threading) instead of OmniThreadLibrary.
{$DEFINE USE_OMNI_THREAD_LIBRARY}

// Comment out to build without the Spring4D dependency.
// When undefined, TParallelFileScannerSpring is not compiled and the demo app
// disables its "Spring container parallel scan" button.
{$DEFINE USE_SPRING4D}
